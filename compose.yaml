services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: trinity_reborn
      POSTGRES_USER: trinity
      POSTGRES_PASSWORD: trinity
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trinity -d trinity_reborn"]
      interval: 2s
      timeout: 3s
      retries: 15

